<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealDishMapper">
    <!--批量插入-->
    <insert id="insertBatch" parameterType="com.sky.entity.SetmealDish">
        insert into setmeal_dish(setmeal_id, dish_id, name, price, copies) VALUES
        <foreach collection="setmealDishes" item="setmealDish" separator="," close="">
            (#{setmealDish.setmealId},#{setmealDish.dishId},#{setmealDish.name},#{setmealDish.price},#{setmealDish.copies})
        </foreach>
    </insert>
    <delete id="deleteBySetmealIds" parameterType="java.util.ArrayList">
        DELETE FROM setmeal_dish WHERE setmeal_id IN
        <foreach item="setmealId" collection="ids" open="(" separator="," close=")">
            #{setmealId}
        </foreach>
    </delete>
    <select id="getSetmealIdsByDishIds" resultType="java.lang.Long">
        SELECT DISTINCT sd.setmeal_id
        FROM setmeal_dish sd
        WHERE sd.dish_id IN
        <foreach item="dishId" collection="array" open="(" separator="," close=")">
            #{dishId}
        </foreach>
    </select>

</mapper>